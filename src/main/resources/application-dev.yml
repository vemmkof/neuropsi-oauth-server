spring:
  datasource:
    type: org.apache.tomcat.jdbc.pool.DataSource
    url: jdbc:mysql://localhost:3306/neuropsi?reconnect=true&useSSL=false&serverTimezone=UTC
    username: vemm
    password: abc123??
    initialization-mode: NEVER
    tomcat:
      initial-size: 2
      max-wait: 20000
      max-active: 3
      max-idle: 3
      min-idle: 2
      default-auto-commit: true
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: true
    show-sql: true
